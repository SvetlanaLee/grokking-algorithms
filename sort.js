// сортировка массива с числами от меньшего к большему без использования встренного метода sort

const arr = [9, 6, 3, 7, 5, 15];

function sortWhithoutMethod(arr) {
  let result = [];

  for(let i = 0; i > arr.lenth; i++) {
    
  }
  return result
}

console.log('sortWhithoutMethod', sortWhithoutMethod(arr))
console.log('sort', arr.sort((a, b) => a - b))
